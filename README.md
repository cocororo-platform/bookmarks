# Bookmark Manager

풀스택 북마크 매니저 — Express + React + SQLite

## Setup

```bash
npm install
cd server && npx prisma migrate dev --name init && cd ..
npm run seed
npm run dev
```

브라우저에서 http://localhost:5173 접속
